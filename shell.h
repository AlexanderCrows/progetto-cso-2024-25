//Definisco i comandi per la shell


void list();
//void man(char* cmd);

int format(char* fs_filename,int size);
int mkdir(char* dirname);
int cd(char* dirname);
int touch(char* fs_filename);
int cat(char* fs_filename);
int ls();
int append();
int rm();
